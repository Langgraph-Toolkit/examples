/**
 * StruxJS convention module. The scanner loads this definition during local
 * development and the emitted index.js after TypeScript compilation.
 */
export { databaseChatDefinition as default } from "../../../../shared/agent.js";
